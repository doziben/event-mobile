type Permissions = "premium" | "elite";

//** For checking if the vendor has permission of a particular priviledge */
export default function useServicePermissions(
  permissions: Permissions[],
  uid?: string
) {}
